import { MovablePositionDirective } from './movable-position.directive';

describe('MovablePositionDirective', () => {
  it('should create an instance', () => {
    const directive = new MovablePositionDirective();
    expect(directive).toBeTruthy();
  });
});
