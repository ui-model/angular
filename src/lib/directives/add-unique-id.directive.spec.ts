import { AddUniqueIdDirective } from './add-unique-id.directive';

describe('AddUniqueIdDirective', () => {
  it('should create an instance', () => {
    const directive = new AddUniqueIdDirective();
    expect(directive).toBeTruthy();
  });
});
